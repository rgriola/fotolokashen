/**
 * @deprecated Import from `@/lib/storage` instead.
 *
 * This file is kept as a thin backward-compatibility re-export so any
 * import path we missed during the storage-adapter migration keeps working.
 * New code must not import from here — use `@/lib/storage`.
 */
export * from './storage';
