import { MapsLayout } from '@/components/layout/MapsLayout';

export default function CreateWithPhotoLayout({ children }: { children: React.ReactNode }) {
    return <MapsLayout>{children}</MapsLayout>;
}
